---
# handlers file for Docker

- name: Restart Docker
  become: true
  ansible.builtin.service:
    name: docker
    state: "{{ docker_engine_restart_handler_state }}"
  when:
    - docker_engine_service_manage | bool
    - docker_engine_allow_restart | bool
